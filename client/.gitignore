# Flutter / Dart
build/
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
*.iml

# Rust
target/
**/*.rs.bk
Cargo.lock.orig

# Nix / direnv
.direnv/
result
result-*

# Android
*.keystore
local.properties
.gradle/

# Editor / OS
.idea/
.vscode/
*.swp
.DS_Store

# flutter_test writes mismatch artifacts here on a golden failure. They are
# debugging output, not fixtures — one set got committed on this branch already.
test/goldens/failures/
